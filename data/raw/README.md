# Raw Data

This directory is reserved for source data snapshots that are intentionally managed by the modernized pipeline.

Current legacy source artifacts still live in:

- `Inputs/`
- `legacy/housing-plus-transportation/Inputs/`
- `Logs/`
